import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Home View")
                .font(.largeTitle)
                .foregroundColor(.black)
            Spacer()
        }
    }
}

#Preview {
    HomeView()
}
