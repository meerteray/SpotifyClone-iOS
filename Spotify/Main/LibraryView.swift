import SwiftUI

struct LibraryView: View {
    var body: some View {
           VStack {
               Text("Library View")
                   .font(.largeTitle)
                   .foregroundColor(.black)
               Spacer()
           }
       }
   }
#Preview {
    LibraryView()
}
