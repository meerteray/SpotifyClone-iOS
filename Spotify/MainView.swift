import SwiftUI

struct MainView: View {
    var body: some View {
        VStack {
            Text("Hello")
                .font(.largeTitle)
                .padding()
        }
    }
}

#Preview {
    MainView()
}
